# ─── AMPLIFY (disabled — connect GitHub manually via Amplify Console) ─────────
# Uncomment when ready to manage via Terraform.
#
# resource "aws_iam_role" "amplify" { ... }
# resource "aws_amplify_app" "dashboard" { ... }
# resource "aws_amplify_branch" "main" { ... }
